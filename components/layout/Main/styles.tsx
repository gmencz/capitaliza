import styled from 'styled-components'

export const Main = styled.main`
  display: flex;
  flex-direction: column;

  p {
    font-size: 1.9rem;
    font-weight: 500;
    text-align: justify;
  }

  .hero {
    font-size: 2.75rem;
    color: white;

    h4 {
      margin-bottom: 0.5em;
    }
  }
`
